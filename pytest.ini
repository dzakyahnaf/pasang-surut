[pytest]
# Kode backend tinggal di backend/, bukan di akar repo, sesuai struktur
# PLAN.md bagian 5. Dua baris berikut membuat `pytest -q` dari akar repo
# menemukan uji dan bisa mengimpor paket `app` tanpa perlu dipasang lebih
# dulu sebagai paket.
testpaths = backend/tests
pythonpath = backend
