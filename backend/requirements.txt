# PASANG SURUT — dependency backend, versi dikunci.
#
# Python 3.11 (lihat .python-version). Versi di bawah adalah hasil resolusi
# nyata pada 23 Agustus 2026 dan sudah diuji terpasang bersama tanpa konflik.
# Aturan repo: tidak ada dependency baru tanpa alasan tertulis. Setiap baris
# di sini punya alasannya.

# ── API ────────────────────────────────────────────────────────────────
fastapi==0.141.1          # framework API, dokumentasi OpenAPI otomatis
uvicorn==0.52.4           # server ASGI yang menjalankan FastAPI

# ── Geospasial ─────────────────────────────────────────────────────────
osmnx==2.1.1              # unduh jaringan jalan OpenStreetMap jadi graf
networkx==3.6.1           # struktur graf + algoritma lintasan terpendek
geopandas==1.1.4          # DataFrame yang punya kolom geometri
rasterio==1.4.4           # baca raster DEMNAS
shapely==2.1.2            # operasi geometri (potong, jarak, dalam-poligon)
pyproj==3.7.2             # alih proyeksi antar CRS, mis. 4326 ke 32749

# ── Citra satelit ──────────────────────────────────────────────────────
# Alasan tertulis, sesuai aturan repo yang melarang dependency tanpa dasar:
# PLAN.md bagian 4 mengunci Google Earth Engine sebagai sumber label
# genangan, dan paket ini satu-satunya cara memanggilnya dari Python.
# Diuji terpasang bersama 14 paket lain tanpa konflik pada 28 Agustus 2026.
#
# CATATAN: geemap SENGAJA TIDAK dipakai. Versi terbarunya menuntut Python
# 3.12 sementara proyek ini dikunci 3.11, dan versi 0.37.2 yang masih cocok
# menarik lebih dari 70 paket tambahan termasuk matplotlib dan ipywidgets.
# Pipeline kita mengekspor tabel, bukan menggambar peta di notebook.
earthengine-api==1.7.41   # Earth Engine Python API

# ── Model & data ───────────────────────────────────────────────────────
scikit-learn==1.9.0       # gradient boosting untuk model genangan
pandas==3.0.5             # tabel fitur latih
numpy==2.4.6              # aritmetika larik, pondasi paket-paket di atas

# ── Database & konfigurasi ─────────────────────────────────────────────
psycopg2-binary==2.9.12   # driver PostgreSQL untuk Supabase
python-dotenv==1.2.3      # baca .env tanpa menaruh secret di kode

# ── Uji ────────────────────────────────────────────────────────────────
pytest==9.1.1             # test runner
